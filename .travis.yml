language: java
jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2/repository/

deploy:
  - provider: script
    script: "mvn -s .travis.settings.xml -DskipTests deploy"
    skip_cleanup: true
    on:
      branch: master
